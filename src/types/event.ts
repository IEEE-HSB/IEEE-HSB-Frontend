export type EventType = {
    id: string,
    name: string,
    link?: string,
    date: string,
    endTime?: string,
    location?: string,
    details?: string,
    moreDetails?: string,
    image: string,
    chapterId: string
}
